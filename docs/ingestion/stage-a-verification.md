# Этап A — отчёт приёмочных проверок

Дата: 2026-09-24T12:34:31+05:00.

**Статус: VERIFIED / PASS в границах этапа A и всего обязательного corpus.**
Ноль необъяснённых потерь и дубликатов. Финальный запуск: `68 passed in 79.36s`; пропусков нет.

Worktree: `C:\Users\tropn\AppData\Local\Temp\kp_ingestion_a`; ветка `feat/canonical-ingestion-a`.
База: `0f49496dc06e361b068d5ebc048f2028da506bde`.
Fingerprint исходников extractor + QA + manifest: `467425f73deabe188400aa5aeb9322b61c102bf0751b40ec2dd5d58f6a13258e`.

## Метрики всех частей документа

Для DOC показаны физические объекты проверенной DOCX-конверсии. Оригинал DOC сохранён отдельно. Блоки включают вложенные абзацы; таблицы не дублируют их текст. Число списков дано как «абзацы / экземпляры нумерации».

| Документ | Формат | Блоки | Абзацы | Таблицы | Ячейки физ./лог. | Объединения | Списки абз./экз. | Кандидаты года |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| КЛЮЧ | DOC | 929 | 924 | 5 | 176 / 169 | 11 | 207 / 29 | 11 |
| Туристы | DOCX | 663 | 660 | 3 | 327 / 320 | 11 | 17 / 3 | 7 |
| Скалолазание | DOCX | 703 | 699 | 4 | 156 / 154 | 3 | 110 / 19 | 0 |
| Природный материал | DOC | 759 | 752 | 7 | 332 / 325 | 19 | 153 / 27 | 23 |
| Ориентирование | DOC | 621 | 618 | 3 | 307 / 303 | 9 | 113 / 6 | 7 |

## Пофайловые доказательства и неоднозначности

### КЛЮЧ

Файл: `Программа КЛЮЧ.DOC`.
SHA-256 оригинала: `c81788305191bbabf7eb911f2b12a4ce0115c3e44a76316dd0b449feea75fd21`.
SHA-256 проверенного DOCX-пакета: `ed7dcf3fffb812677abbb86baa047d17bfb93eaa76cbb7c609a3d76bedbba8b7`.
SHA-256 структуры: `7ffccf2135d05ba184164e7bf4a715b847822998b4673f675b1396655ca75973`.

Основной текст: 860 абзацев. Все части: 924 абзацев; 18136 XML-узлов, 898 текстовых/служебных фрагментов, 181 позиций сетки. Неизвестных блоков: 0. Диагностик extractor: 0.
Повторяющихся непустых абзацев: 284; каждый имеет собственный исходный адрес и ID.

PASS: каждый абзац, таблица, ячейка, XML-узел и часть присутствуют; порядок и вложенность совпадают с независимым XML-инвентарём; текст вложенного абзаца не копируется в родителя. Объединения дополнительно совпали с сеткой python-docx. Повтор и переименование дали те же ID и структуру. Назначения года нет.

Необъяснённые потери: **0**. Необъяснённые дубликаты: **0**.

DOC→FODT и DOC→DOCX: последовательность 711 непустых абзацев совпала; совпали 5 таблиц, 169 логических ячеек и 11 объединений.
Независимое чтение Word 16.0: те же 711 непустых блоков основного текста до/после, порядок и текст совпали.
Пустые абзацы в конверсии сверх FODT: 8. Причины: empty paragraph required in vertical merge continuation cell: 7; empty paragraph carrying a section break: 1. Все адреса записаны в JSON-журнале.
Исходные координаты DOC адресуются через DOCX-конверсию и журнал хешей; бинарные смещения DOC не выдаются за XML-адреса.
Служебная часть 9: field instruction preserved; cached display may change and compatibility branches remain separate; поля PAGE.
Служебная часть 12: automatic footnote/endnote separator control; no content text.
Служебная часть 13: automatic footnote/endnote separator control; no content text.
Служебная часть 15: automatic footnote/endnote separator control; no content text.
Служебная часть 16: automatic footnote/endnote separator control; no content text.
Сообщение конвертера (не скрыто): `Could not find platform independent libraries <prefix>`. Статус 0, обе независимые сверки пройдены.

Неоднозначность представления: 32 ветвей Choice/Fallback сохранены раздельно. Это разные исходные XML-объекты, а не дублирование, созданное extractor. Выбор отображаемой ветви здесь не выполняется.

Конвертер создал 32 части колонтитулов для ссылок разделов; исходная инструкция PAGE сохранена внутри текстовых рамок в альтернативных XML-представлениях. Кеш отображаемого номера страницы может отличаться от DOC; это служебное вычисляемое поле, исходные байты не менялись.

### Туристы

Файл: `Программа ТУРИСТЫ-ПРОВОДНИКИ 1 г.docx`.
SHA-256 оригинала: `f985490b127b4ce13e86b68a08a4973b2a6d92191c554934f4dc431a92626dcd`.
SHA-256 проверенного DOCX-пакета: `f985490b127b4ce13e86b68a08a4973b2a6d92191c554934f4dc431a92626dcd`.
SHA-256 структуры: `35e9d9034b54131ebfb1178b8f4d78c83f1ba85a2a329cf2285ccb32d5ba099d`.

Основной текст: 654 абзацев. Все части: 660 абзацев; 17367 XML-узлов, 967 текстовых/служебных фрагментов, 345 позиций сетки. Неизвестных блоков: 0. Диагностик extractor: 0.
Повторяющихся непустых абзацев: 221; каждый имеет собственный исходный адрес и ID.

PASS: каждый абзац, таблица, ячейка, XML-узел и часть присутствуют; порядок и вложенность совпадают с независимым XML-инвентарём; текст вложенного абзаца не копируется в родителя. Объединения дополнительно совпали с сеткой python-docx. Повтор и переименование дали те же ID и структуру. Назначения года нет.

Необъяснённые потери: **0**. Необъяснённые дубликаты: **0**.

Неразрешённых технических неоднозначностей извлечения не выявлено. Семантика текста не определялась.

### Скалолазание

Файл: `Программа Скалолазание .docx`.
SHA-256 оригинала: `1832c30a8f2ef6e3d48d4a2b3f15444d474fa9915a515002c6efa1506802cc94`.
SHA-256 проверенного DOCX-пакета: `1832c30a8f2ef6e3d48d4a2b3f15444d474fa9915a515002c6efa1506802cc94`.
SHA-256 структуры: `ce4d2531384c23f795b3771b0dee68b12cf91f82dff507687427c39638594d0b`.

Основной текст: 680 абзацев. Все части: 699 абзацев; 22049 XML-узлов, 3174 текстовых/служебных фрагментов, 158 позиций сетки. Неизвестных блоков: 0. Диагностик extractor: 0.
Повторяющихся непустых абзацев: 280; каждый имеет собственный исходный адрес и ID.

PASS: каждый абзац, таблица, ячейка, XML-узел и часть присутствуют; порядок и вложенность совпадают с независимым XML-инвентарём; текст вложенного абзаца не копируется в родителя. Объединения дополнительно совпали с сеткой python-docx. Повтор и переименование дали те же ID и структуру. Назначения года нет.

Необъяснённые потери: **0**. Необъяснённые дубликаты: **0**.

Неоднозначность представления: 10 ветвей Choice/Fallback сохранены раздельно. Это разные исходные XML-объекты, а не дублирование, созданное extractor. Выбор отображаемой ветви здесь не выполняется.

Явные маркеры по грамматике этапа A не найдены. Год не угадан и содержанию не назначен; все исходные фразы сохранены для последующих этапов.

### Природный материал

Файл: `Работа с природным материалом.doc`.
SHA-256 оригинала: `403893733ff5409d366b201a54f3ca8aa7168d1ba51f685d497d094d6616ef00`.
SHA-256 проверенного DOCX-пакета: `be6642fca3beed22dce9c3f16729808423e0b05dcbd37b826b3ff264804678c4`.
SHA-256 структуры: `f5f59126f99c185edd212cc480b6621fb600ef8f7474df48abd8c7477b1cba85`.

Основной текст: 752 абзацев. Все части: 752 абзацев; 15207 XML-узлов, 744 текстовых/служебных фрагментов, 368 позиций сетки. Неизвестных блоков: 0. Диагностик extractor: 0.
Повторяющихся непустых абзацев: 145; каждый имеет собственный исходный адрес и ID.

PASS: каждый абзац, таблица, ячейка, XML-узел и часть присутствуют; порядок и вложенность совпадают с независимым XML-инвентарём; текст вложенного абзаца не копируется в родителя. Объединения дополнительно совпали с сеткой python-docx. Повтор и переименование дали те же ID и структуру. Назначения года нет.

Необъяснённые потери: **0**. Необъяснённые дубликаты: **0**.

DOC→FODT и DOC→DOCX: последовательность 600 непустых абзацев совпала; совпали 7 таблиц, 325 логических ячеек и 19 объединений.
Независимое чтение Word 16.0: те же 600 непустых блоков основного текста до/после, порядок и текст совпали.
Пустые абзацы в конверсии сверх FODT: 9. Причины: empty paragraph carrying a section break: 2; empty paragraph required in vertical merge continuation cell: 7. Все адреса записаны в JSON-журнале.
Исходные координаты DOC адресуются через DOCX-конверсию и журнал хешей; бинарные смещения DOC не выдаются за XML-адреса.
Сообщение конвертера (не скрыто): `Could not find platform independent libraries <prefix>`. Статус 0, обе независимые сверки пройдены.

### Ориентирование

Файл: `Программа по Ориентированию для нач.кл..doc`.
SHA-256 оригинала: `8b97f98735cde648cba3970283b1a58cd2bf17459d626cf15ba3018df096a02b`.
SHA-256 проверенного DOCX-пакета: `deda75ae63043e58a3d4b0192b96637e31cf61efafda31004fba16ba7d85529b`.
SHA-256 структуры: `11459ee084752fbcdb790b9dbea94107ef78d974f3cfa413f1a7ff57007e6861`.

Основной текст: 618 абзацев. Все части: 618 абзацев; 11441 XML-узлов, 653 текстовых/служебных фрагментов, 318 позиций сетки. Неизвестных блоков: 0. Диагностик extractor: 0.
Повторяющихся непустых абзацев: 232; каждый имеет собственный исходный адрес и ID.

PASS: каждый абзац, таблица, ячейка, XML-узел и часть присутствуют; порядок и вложенность совпадают с независимым XML-инвентарём; текст вложенного абзаца не копируется в родителя. Объединения дополнительно совпали с сеткой python-docx. Повтор и переименование дали те же ID и структуру. Назначения года нет.

Необъяснённые потери: **0**. Необъяснённые дубликаты: **0**.

DOC→FODT и DOC→DOCX: последовательность 548 непустых абзацев совпала; совпали 3 таблиц, 303 логических ячеек и 9 объединений.
Независимое чтение Word 16.0: те же 548 непустых блоков основного текста до/после, порядок и текст совпали.
Пустые абзацы в конверсии сверх FODT: 4. Причины: empty paragraph required in vertical merge continuation cell: 4. Все адреса записаны в JSON-журнале.
Исходные координаты DOC адресуются через DOCX-конверсию и журнал хешей; бинарные смещения DOC не выдаются за XML-адреса.
Служебная часть 12: automatic footnote/endnote separator control; no content text.
Служебная часть 13: automatic footnote/endnote separator control; no content text.
Служебная часть 15: automatic footnote/endnote separator control; no content text.
Служебная часть 16: automatic footnote/endnote separator control; no content text.
Сообщение конвертера (не скрыто): `Could not find platform independent libraries <prefix>`. Статус 0, обе независимые сверки пройдены.

## Проверки и границы

- 68 автоматических тестов: 62 дополнительных property/invariant/error checks и 6 проверок единого реального corpus.
- 24 случайные последовательности абзацев/таблиц/контейнеров, 16 случайных разбиений сетки объединений; повторяющийся текст, вложенность, стили/списки, неизвестные блоки, XML-альтернативы, лимиты и таймаут реального DOC-конвертера.
- Отсутствующий или изменённый реальный файл завершает gate ошибкой. Синтетические документы не заменяют corpus.
- Все исходные SHA перепроверены; в финальном gate Word открывал временные копии read-only и ничего не сохранял.
- Структурные проверки не являются визуальной проверкой пагинации. Физические XML-ветви и служебные поля не превращались в учебное содержание.
- Existing downstream не изменён и не подключён. УТП, matching, распределение SOURCE, недели, RESULT/CONTROL и календарный DOCX не создавались.
- Полный машинный журнал, SourceSpan объяснённых добавлений, stdout/stderr и метрики частей: `_shadow_out/lossless_stage_a_report.json` (локальный воспроизводимый QA-артефакт).

Переход к этапу B требует отдельного подтверждения пользователя. Push/deploy/production switch не выполняются.
